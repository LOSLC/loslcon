import type { NextConfig } from "next";

const nextConfig: NextConfig = {
  // Future: could configure i18n routing; for now we use client-side toggle
  reactStrictMode: true,
};

export default nextConfig;
